name: Build & Verify FastAPI App

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          echo "✅ Dependencies installed successfully"

      - name: Verify bcrypt/passlib versions
        run: |
          python -c "import bcrypt, passlib; print('bcrypt version:', getattr(bcrypt, '__version__', 'unknown'))"
          python -c "from passlib.hash import bcrypt; print('passlib-bcrypt ok:', bcrypt.hash('test123'))"

      - name: Test PostgreSQL connectivity
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          pip install psycopg2-binary
          python - <<'PYCODE'
          import os, psycopg2
          url = os.environ["DATABASE_URL"]
          conn = psycopg2.connect(url)
          cur = conn.cursor()
          cur.execute("SELECT NOW();")
          print("✅ Database connected:", cur.fetchone())
          conn.close()
          PYCODE

      - name: Validate FastAPI app import
        run: |
          python - <<'PYCODE'
          import importlib
          app = importlib.import_module("main")
          print("✅ FastAPI app loaded:", hasattr(app, "app"))
          PYCODE

      - name: Summary
        run: echo "🎉 Build verified successfully with psycopg2-binary + bcrypt 4.0.1"
